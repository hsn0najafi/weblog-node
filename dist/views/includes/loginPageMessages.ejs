<% if (message.length > 0) { %>
<div class="errorMessage">
  <!-- ------------------- -->
  <p><%= message %></p>
  <!-- ---------- -->
</div>
<% } %>

<!-- ---------------------- -->

<% if (error.length > 0) { %>
<div class="errorMessage">
  <!-- ------------------- -->
  <p><%= error %></p>
  <!-- ---------- -->
</div>
<% } %>
